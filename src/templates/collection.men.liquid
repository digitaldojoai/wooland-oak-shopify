{% paginate collection.products by 100 %}

{% section 'collection-hero-men' %}

<section class='outer' data-component='collection' data-collection='{{ collection.handle }}'>
  <div class='container'>
    <div id='collectionRoot'></div>
  </div>

  {% section 'collection-features-men' %}
</section>

{% endpaginate %}

{% include 'newsletter' with border: true %}
